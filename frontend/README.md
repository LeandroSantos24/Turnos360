# Turnos360 — Frontend

Next.js (App Router) + TypeScript + Tailwind CSS 4.

```bash
npm install
npm run dev   # http://localhost:3000
```

En E3 acá vive el panel del negocio (agenda con React Big Calendar);
en E4, la landing pública por slug (`app/(public)/[slug]`).

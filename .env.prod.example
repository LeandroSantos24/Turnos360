# Plantilla del .env.prod. Copiar al servidor como .env.prod y completar.
# No se versiona (.gitignore excluye .env*).
#
# Generar secretos:  python3 -c "import secrets; print(secrets.token_urlsafe(48))"

# ── Base de datos y Redis ──────────────────────────────────────────────────
POSTGRES_USER=turnos360
POSTGRES_PASSWORD=
POSTGRES_DB=turnos360
REDIS_PASSWORD=                 # solo alfanumérico: viaja dentro de una URL

# ── Secretos ───────────────────────────────────────────────────────────────
SECRET_KEY=                     # firma de los JWT
FERNET_KEY=                     # cifra las credenciales guardadas. Distinta de
                                # SECRET_KEY. Rotarla deja ilegible lo guardado.

# ── Super-admin (primer seed) ──────────────────────────────────────────────
SUPERADMIN_EMAIL=
SUPERADMIN_PASS=

# ── URLs ───────────────────────────────────────────────────────────────────
# NEXT_PUBLIC_API_URL se hornea en el bundle: si cambia, rebuildear el front.
NEXT_PUBLIC_API_URL=
PUBLIC_BASE_URL=
API_BASE_URL=
CORS_ORIGINS=                   # separados por coma

# ── Entorno ────────────────────────────────────────────────────────────────
ENV=prod                        # dev | staging | prod
ZONA_HORARIA=America/Argentina/Buenos_Aires
UVICORN_WORKERS=2
LOG_JSON=true                   # una línea de JSON por evento, para filtrar con jq
SENTRY_DSN=                     # opcional; vacío = te enterás por un cliente

# ── Email saliente ─────────────────────────────────────────────────────────
SMTP_USER=
SMTP_PASS=                      # contraseña de aplicación, sin espacios
SMTP_FROM=

# ── Mercado Pago: firma de los webhooks ────────────────────────────────────
# off | log | enforce. Camino: log unos días → mirar los logs → enforce.
MP_FIRMA_MODO=off
MP_WEBHOOK_SECRET=              # MP → Tus integraciones → Webhooks → Clave secreta

# ── WhatsApp ───────────────────────────────────────────────────────────────
# simulado | meta. En simulado no sale ningún mensaje a la calle.
WA_PROVEEDOR=simulado
WA_WEBHOOK_VERIFY_TOKEN=
WA_APP_SECRET=
WA_PRECIO_MENSAJE_ARS=73        # precio de venta de un mensaje
WA_COBRAR_SERVICIO=false        # true desde el 01/10/2026 (Meta empieza a cobrarlos)

# ── Tus datos de cobro (los ve el negocio en "Mi suscripción") ─────────────
COBRO_CBU=
COBRO_ALIAS=
COBRO_TITULAR=
COBRO_CUIT=
COBRO_BANCO=
COBRO_MP_LINK=
COBRO_WHATSAPP=                 # formato wa.me, sin +

# ── Precios ────────────────────────────────────────────────────────────────
# Si cambian, actualizar también frontend/src/lib/precios.ts (van en el bundle).
PRECIO_LISTA_MENSUAL=14990
PROMO_ACTIVA=false
PRECIO_PROMO_MENSUAL=11990
PROMO_ETIQUETA=Precio de lanzamiento

# ── Aviso de acceso al panel de super-admin ────────────────────────────────
ADMIN_ALERTA_EMAIL=             # vacío = no se manda nada
ADMIN_ALERTA_FALLIDOS=true
ADMIN_ALERTA_EN_DEV=false
ADMIN_ALERTA_GEOLOCALIZAR=true
